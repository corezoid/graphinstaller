{{- if .Values.global.graphinstaller.graphinstaller.autoscaling.enabled }}
apiVersion: {{ include "common.capabilities.hpa.apiVersion" ( dict "context" $ ) }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "graphinstaller.fullname" . }}
  labels:
    {{- include "graphinstaller.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "graphinstaller.fullname" . }}
  minReplicas: {{ .Values.global.graphinstaller.graphinstaller.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.global.graphinstaller.graphinstaller.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.global.graphinstaller.graphinstaller.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.global.graphinstaller.graphinstaller.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.global.graphinstaller.graphinstaller.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.global.graphinstaller.graphinstaller.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
